// Pantalla del juego (resultado del navegador)
const GameViewport = () => {
  return (
    <div>        
        <canvas id="viewportGame"></canvas>
    </div>
    
  )
}

export default GameViewport
